name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release-please:
    uses: DrinkBetter-AI/github-actions/.github/workflows/release-please.yml@main
    secrets: inherit
